data:extend(
{
  { --infinite steam (i.e. works like oil, if disabled steam vents will run out eventually)
    type = "bool-setting",
    name = "infinite-steam-resource",
    setting_type = "startup",
    default_value = true,
  },
}
)